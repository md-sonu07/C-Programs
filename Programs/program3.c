// Program 3: Input and Display Numbers
#include <stdio.h>
#include <conio.h>

int main() 
{
    int a, b;
    clrscr();
    printf("Enter two numbers: ");
    scanf("%d%d", &a, &b);
    printf("The Value of a=%d and b=%d", a, b);
    getch();
    return 0;
}
